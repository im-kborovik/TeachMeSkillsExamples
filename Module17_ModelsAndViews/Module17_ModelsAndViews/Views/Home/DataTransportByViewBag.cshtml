@{
    var users = ViewBag.Users as User[];
    ViewBag.Title = "DataTransportByViewBag";
}

<ul>
    @foreach (var user in users)
    {
        <li>@("Name: " + user.Name + " Age: " + user.Age + " City: " + user.City)</li>
    }
</ul>
